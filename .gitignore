# Build output
[Bb]in/
[Oo]bj/
publish/
publish-linux/
Releases/
*.user
.vs/

# Cau hinh ky so thuc (theo tung tai khoan Azure) — giu ban .example, khong commit ban that
signing/trusted-signing.json

# Runtime data (profile trình duyệt, db, cache — tạo lại khi chạy)
persistent-data/
*.db
*.db-shm
*.db-wal

# Rider / VS
.idea/
*.suo

# Logs / temp
*.log
*.tmp

# Công cụ dev cục bộ (chỉ chạy trên máy này, không đưa lên repo)
tools/scrape-test-agent/

# Thu muc lam viec tam / build artifacts
.cursor/
artifacts/
scratchpad/

# Du lieu runtime cua hub web khi chay LOCAL — TUYET DOI khong commit (repo la public):
#   hub-data/dp-keys/ = khoa Data Protection (XML chua khoa doi xung)
#   hub-data/files/   = kho cookie/workbook client sync len
server/Shopee.Hub.Web/hub-data/

# Ket qua chay test (dotnet test --results-directory)
test-results/
