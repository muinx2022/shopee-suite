<UserControl x:Class="Shopee.Suite.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:Shopee.Suite.Controls"
             x:Name="Root">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="40,36">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Hero -->
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <Border Width="56" Height="56" CornerRadius="6" Background="{StaticResource BrandBadgeBrush}">
                    <TextBlock Text="S" FontSize="30" FontWeight="Bold" Foreground="White"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel Margin="18,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Shopee Suite" FontSize="28" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" />
                    <TextBlock Style="{StaticResource caption}" Margin="0,2,0,0"
                               Text="Bộ công cụ hợp nhất — quản lý tài khoản, scrape, tìm kiếm và cập nhật sản phẩm Shopee." />
                </StackPanel>
            </StackPanel>

            <TextBlock Grid.Row="1" Style="{StaticResource h2}" Text="Bắt đầu" Margin="2,32,0,12" />

            <!-- Module tiles -->
            <ItemsControl Grid.Row="2" ItemsSource="{Binding Tiles}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource cardButton}" Width="300" Margin="0,0,16,16"
                                Command="{Binding DataContext.OpenCommand,
                                          RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                CommandParameter="{Binding}">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="44" Height="44" CornerRadius="6" Background="{StaticResource SubtleBrush}"
                                        VerticalAlignment="Top">
                                    <c:PathIcon Data="{Binding Icon}" Width="23" Height="23"
                                                Foreground="{StaticResource AccentBrush}"
                                                HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <StackPanel Margin="14,0,0,0" VerticalAlignment="Center" Width="190">
                                    <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimaryBrush}" />
                                    <TextBlock Style="{StaticResource caption}" Text="{Binding Subtitle}" Margin="0,2,0,0" />
                                </StackPanel>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ScrollViewer>
</UserControl>
