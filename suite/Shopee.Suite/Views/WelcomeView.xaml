<UserControl
    x:Class="Shopee.Suite.Views.WelcomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Name="Root">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="40,36,40,36">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Hero -->
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <Border Width="56" Height="56" CornerRadius="14" Background="{StaticResource AccentBrush}">
                    <TextBlock Text="S" FontSize="30" FontWeight="Bold" Foreground="White"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel Margin="18,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Shopee Suite" FontSize="28" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" />
                    <TextBlock Text="Bộ công cụ hợp nhất — quản lý tài khoản, scrape, tìm kiếm và cập nhật sản phẩm Shopee."
                               Style="{StaticResource Caption}" Margin="0,2,0,0" />
                </StackPanel>
            </StackPanel>

            <TextBlock Grid.Row="1" Text="Bắt đầu" Style="{StaticResource H2}" Margin="2,32,0,12" />

            <!-- Module tiles -->
            <ItemsControl Grid.Row="2" ItemsSource="{Binding Tiles}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource CardButton}" Width="300" Margin="0,0,16,16"
                                Command="{Binding DataContext.OpenCommand, ElementName=Root}"
                                CommandParameter="{Binding}">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="44" Height="44" CornerRadius="11" Background="{StaticResource SubtleBrush}"
                                        VerticalAlignment="Top">
                                    <TextBlock Text="{Binding Glyph}" FontFamily="Segoe UI Emoji" FontSize="22"
                                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <StackPanel Margin="14,0,0,0" VerticalAlignment="Center" Width="190">
                                    <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimaryBrush}" />
                                    <TextBlock Text="{Binding Subtitle}" Style="{StaticResource Caption}" Margin="0,2,0,0" />
                                </StackPanel>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ScrollViewer>
</UserControl>
